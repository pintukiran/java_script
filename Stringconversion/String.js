//CONVERSION:-
//The word conversion refers to either implicitly or explicitly changeing a value from one data type to another

//COERCION:-
//The word coercion is used to implicit(automatic) conversion 

//-------------------String conversion----------------//

let x=String(908+1);
console.log(x)